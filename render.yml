services:
  - type: web
    name: pontia-mlops-tutorial-diego-gilsanchez
    env: python
    region: frankfurt
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn deployment.app.main:app --host 0.0.0.0 --port 8080
    # Para que Render sepa qué puerto exponer
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
